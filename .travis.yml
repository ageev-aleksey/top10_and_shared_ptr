language: cpp
compiler: g++
script:
        - apt-get install cmake
        - mkdir build
        - cd tests
        - git clone https://github.com/google/googletest.git
        - cd ../build
        - cmake ..
        - cmake --build .



